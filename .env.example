# Gemini Configuration
GEMINI_API_KEY=AIzaSy... (Get from Google AI Studio)

# HydraDB Configuration
HYDRADB_API_KEY=sk_live_... (Get from HydraDB dashboard)

# --- Backend Deployment (Render / Vercel API) ---
PORT=3000
DATABASE_URL=postgresql://user:password@host/db?sslmode=require
CORS_ORIGIN=https://usedaisy.vercel.app

# Frontend Config (Vite)
# Point this to your Render/Backend URL in production (e.g., https://daisy-backend.onrender.com/api)
VITE_API_BASE_URL=/api
